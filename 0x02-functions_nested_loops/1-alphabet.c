#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - check the code
 * Description: prints the alphabet, in lowercase
 * Return: Always 0
 */
void alphabet(void)
{
char  k = 'a';

for (k = 'a'; k <= 'z'; k++)
{
	_putchar(k);
}
	_putchar('\n');
